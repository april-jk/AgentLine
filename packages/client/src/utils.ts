// Runtime utilities re-exported for convenience
// Keep types in types.ts, utilities here

// Branded ID conversions
export { toUrlProjectId, fromUrlProjectId } from "@agentline/shared";

// Session display helpers
export { getSessionDisplayTitle } from "@agentline/shared";

// Message helpers
export { getMessageId } from "./lib/mergeMessages";
